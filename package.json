{
  "name": "foreman",
  "version": "0.1.0",
  "private": true,
  "description": "AI Agent Supervisor System - Enforce task scope on coding agents",
  "keywords": ["ai", "agent", "supervisor", "mcp", "git-hooks", "code-review"],
  "author": "Peter Schings",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "prettier": "^3.2.4",
    "eslint": "^8.56.0"
  }
}

